# &CONTROL

## 文档目的

解释计算类型、文件路径、restart、输出控制和 HPC 运行相关参数。

## 关键参数

`calculation`、`prefix`、`outdir`、`pseudo_dir`、`restart_mode`、`max_seconds`、`tstress`、`tprnfor`。

## 相关 workflow

SCF、relax、vc-relax、bands、NSCF、HPC restart。

## 资料状态

需官方 `INPUT_PW.html` 逐项核对默认值与版本差异。

